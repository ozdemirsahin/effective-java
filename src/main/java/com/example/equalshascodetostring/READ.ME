primiteve type'lar == operatörü ile karşılaştırılırken,referans type'ların değerlerini karşılaştırmak için equals methodu kullanılır.

equals methods;

reflexive: an object must equal itself x.equals(x)
symmetric: x.equals(y) must return the same result as y.equals(x)
transitive: if x.equals(y) and y.equals(z) then also x.equals(z)
consistent: the value of equals() should change only if a property that is contained in equals() changes (no randomness allowed)
null control a.equals(null)
